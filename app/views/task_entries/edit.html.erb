<% provide(:title, 'Update Track Time') %>
<h1>Update your Time</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@task_entry) do |f| %>

      <%= f.label "Select Task" %>
      <%= f.collection_select(:task_id, Task.all, :id, :task_name) %>

      <%= f.label "Notes" %>
      <%= f.text_area :note, class: 'form-control' %>

      <%= f.label "Start Time" %>
      <%= f.datetime_field :start_time, class: 'form-control' %>

      <%= f.label "Stop Time" %>
      <%= f.datetime_field :stop_time, class: 'form-control' %><br>

      <%= f.submit "Update Tracking", class: "btn btn-primary" %>

    <% end %>
  </div>
</div>
